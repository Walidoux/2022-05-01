# Examples

Those events will be triggered/emitted:

```csharp
on first join: // when user joins the room for the first time
on room enter/leave: // when user joins/leaves the room
on region enter/leave: // when user enters/leaves a region
on tile click: // when user releases a click on a tile
on vehicle enter: // when user (i.e) rides a horse
on sit:
on lay:
on talk: // when user sends a message and the avatar moves his head
on move: // when user moves around the room

every 2 seconds:
every hour:
every 10 minutes:
```
