# Examples

```cs

if player !|has room permissions:
if player is|is not in region {region}
if players.length ==(!=) 10:
if player !|has $BAGDE_CODE:
if player !|has $ID_ENABLE
if entity !stands on $mobi(s):

```
